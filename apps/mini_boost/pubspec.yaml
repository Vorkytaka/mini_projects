name: mini_boost
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  confetti: ^0.7.0
  flutter:
    sdk: flutter
  home_widget: ^0.5.0
  mini_design:
    path: ../../packages/mini_design
  rxdart: ^0.27.7
  shared_preferences: ^2.2.2
  streaming_shared_preferences: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
